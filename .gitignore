.ipynb_checkpoints/
__pycache__/
.DS_Store
*.pyc
notebooks/*.ipynb
!notebooks/smoke_test.py

# Lesson output data (Parquet / CSV files produced by lessons).
# Keep the directory itself (with a .gitkeep) so containers can mount it.
data/*
!data/.gitkeep
